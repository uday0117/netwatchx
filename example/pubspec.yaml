name: netwatchx_example
description: Example app demonstrating netwatchx package usage
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.4
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

  netwatchx:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
